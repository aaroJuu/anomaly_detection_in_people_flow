def obstacle_zones():
    # Define obstacle zones as (x_min, x_max, y_min, y_max)
    zones = [
        (132.0, 137.5, -170.5, -164.5),   # Lower pillar
        (132.0, 137.5, -155.0, -149.0)    # Upper pillar
    ]

    return zones